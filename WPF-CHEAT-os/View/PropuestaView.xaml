<UserControl x:Class="WPF_CHEAT_os.View.PropuestaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_CHEAT_os.View"
             xmlns:viewModel="clr-namespace:WPF_CHEAT_os.ViewModel"
             mc:Ignorable="d" 
             Background="White"
             d:DesignHeight="450" d:DesignWidth="800">
    

    
    <Grid>
        <TextBlock Text="Propuestas Enviadas por los Alumnos" FontSize="24" HorizontalAlignment="Center" Margin="0,20,0,0"/>

        <DataGrid x:Name="PropuestasDataGrid" Margin="10,60,10,10" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns>
                <!-- <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/> -->
                <DataGridTextColumn Header="ID"  Width="50"/>
                <!-- <DataGridTextColumn Header="Alumno" Binding="{Binding AlumnoNombre}" Width="200"/> -->
                <DataGridTextColumn Header="Alumno" Width="200"/>
                <!-- <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" Width="400"/> -->
                <DataGridTextColumn Header="Descripción"  Width="400"/>
                <!-- <DataGridTextColumn Header="Fecha de Envío" Binding="{Binding FechaEnvio}" Width="150"/> -->
                <DataGridTextColumn Header="Fecha de Envío" Width="150"/>
                <!-- <DataGridTextColumn Header="Estado" Binding="{Binding Estado, Converter={StaticResource EnumToStringConverter}}" Width="100"/> -->
                <DataGridTextColumn Header="Estado" Width="100"/>
                <DataGridTemplateColumn Header="Acciones">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <!--<Button Content="Ver" Click="VerPropuesta_Click"/> -->
                            <Button Content="Ver"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
